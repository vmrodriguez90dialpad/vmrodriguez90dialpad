# Project Documentation Title (H1)

This is a **brief introduction** to the project. Markdown is simple to read and write.

---

## 🚀 Section 1: Key Features (H2)

Here is an unordered list of the most important features:

* **Feature A**: Handles data processing efficiently.
* **Feature B**: Provides a user-friendly interface.
* **Feature C**: Is highly scalable.

### Sub-Section: Implementation Details (H3)

We follow best practices. A simple numbered list:

1.  Initialize the main module.
2.  Load the configuration file.
3.  Start the core service.

---

## 💡 Section 2: Code Example

You can easily embed code blocks.

### Inline Code

To access the variable, use the command `print(variable_name)`.

### Block Code

For a larger example, use three backticks:

```python
def calculate_sum(a, b):
    # This is a Python function
    result = a + b
    return result

print(calculate_sum(5, 10))
